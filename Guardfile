# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'livereload' do
  # watch(%r{application//.+\.(erb|haml|slim)$})
  watch(%r{application/.+\.py})
  watch(%r{application/.+\.(css|js|html)})
  #port: '8080'
  # watch(%r{/.+\.yml})
  # Rails Assets Pipeline
  # watch(%r{(app|vendor)(/assets/\w+/(.+\.(css|js|html|png|jpg))).*}) { |m| "/assets/#{m[3]}" }
end
